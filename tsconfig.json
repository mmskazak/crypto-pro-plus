{
    "compilerOptions": {
      /* ---- базовое ---- */
      "target": "ES2022",            
      "module": "ESNext",                 // tsup сам перезавёрстывает в esm/cjs
      "moduleResolution": "node",
      "lib": ["dom", "es2017"],           // DOM‑API + Promise/Map/Set/…
      "outDir": "dist/types",             // временно; tsup потом кладёт финальные .d.ts в dist/
  
      /* ---- качество кода ---- */
      "strict": true,                     // включаем строгий режим
      "esModuleInterop": true,            // без этого CommonJS импортится больно
      "skipLibCheck": true,               // игнор чужих d.ts — быстрее билд
      "resolveJsonModule": true,          // если вдруг импортируем .json
  
      /* ---- d.ts generation ---- */
      "declaration": true,
      "declarationDir": "dist/types",
      "emitDeclarationOnly": true,
  
      /* ---- source maps ---- */
      "sourceMap": true,
      "inlineSources": true
    },
  
    /* исключаем лишнее из компиляции */
    "exclude": [
      "dist",
      "node_modules",
      "test",
      "**/__mocks__"
    ],
  
    /* указываем, что компилировать */
    "include": ["src/**/*"]
  }
  